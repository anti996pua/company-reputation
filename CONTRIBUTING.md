# 贡献指南

感谢您为本项目贡献数据！本文档说明如何添加/修改公司、部门和职位数据。

## 数据格式

所有数据以 Markdown 文件形式存放在 `companies/` 目录下，按公司名分组。

### 公司

创建 `companies/{公司名}.md`：

```markdown
# 公司全称
======================
公司简介文字...

## 基本信息
- 英文名：EnglishName
- 别名：别名1、别名2
- 标签：大厂、互联网
- 城市：北京、深圳、杭州
- 规模：10000+
- 行业：互联网
- 成立：1998
- 网站：https://www.example.com
```

### 部门

创建 `companies/{公司名}/{部门名}.md`：

```markdown
# 部门全称
======================
部门简介...

## 基本信息
- 城市：北京、杭州
- 规模：3000+
- 部门规模：500+
```

### 职位

在部门目录下创建 `positions/` 子目录：

```
companies/{公司名}/{部门名}/positions/{职位名}.md
```

```markdown
# 职位名称
======================
职位描述...

## 基本信息
- 类别：技术
- 职级范围：P5-P8
- 工作地点：北京、杭州

## 薪资范围
| 职级 | 薪资 |
|------|------|
| P5   | 28K-38K × 16 薪 |

## 技术栈
Java、Spring、Redis、Kafka

## 任职要求
- 本科及以上学历
- 5 年以上相关经验

## 面试流程
1. 简历筛选
2. 技术一面
3. 技术二面
4. HR 面

## 类似职位
- 公司名/部门名/职位名
```

### 员工评价

在公司或部门的 Markdown 中添加 `## 员工评价` 区块：

```markdown
## 员工评价

### 2024-06 | 技术专家 | 3年 | 匿名用户
> 整体不错，技术氛围好。
评分：4.0/5
**优点**：技术栈先进、同事水平高
**缺点**：加班较多、晋升路径不清晰
```

## 分支策略

数据贡献请提交到 `data` 分支（非 `main` 或 `develop`）：

```
main（生产）← data（数据贡献）← develop（开发）
```

详见 [docs/BRANCHING.md](docs/BRANCHING.md)。

## 提交规范

### 分支命名

- 数据贡献：`data/{公司名}`（如 `data/华为`）
- Bug 修复：`fix/{描述}`（如 `fix/search-encoding`）
- 功能开发：`feat/{描述}`（如 `feat/compare-mode`）

### 提交信息格式

```
[emoji] type(scope): 简短描述（50 字以内）

详细说明（可选，72 字换行）
```

**type 类型**：

| type | emoji | 用途 |
|------|-------|------|
| feat | ✨ | 新功能 |
| fix | 🐛 | Bug 修复 |
| docs | 📖 | 文档变更 |
| refactor | ♻️ | 代码重构 |
| perf | ⚡ | 性能优化 |
| test | 🧪 | 测试相关 |
| ci | 👷 | CI/CD 配置 |
| chore | 🔧 | 构建/工具/配置 |
| style | 🎨 | 代码格式 |

### 提交原则

- **小提交**：每个提交只做一件事，一个功能拆成多个逻辑提交
- **完整构建**：每次提交前运行 `npm run build` 确保不破坏构建
- **文档同步**：代码变更同步更新相关文档
- **不做混合提交**：不把功能开发和文档修改混在一个提交里

## 提交前检查

1. 运行 `npm run build` 确保构建通过
2. 运行 `node scripts/validate-data.mjs` 校验数据格式
3. 手动验证受影响的功能页面
4. 确认文档已同步更新

## 开发环境

```bash
# 安装依赖
npm install

# 实时预览（自动监听变化）
npm run dev

# 构建站点
npm run build
```

## 相关文档

- [docs/USER_GUIDE.md](docs/USER_GUIDE.md) — 用户使用指南
- [docs/TESTING.md](docs/TESTING.md) — 测试用例
- [AGENTS.md](AGENTS.md) — AI 助手操作指引
